# SimRT Linux 实时模型与 XCP 上位机需求说明

版本：单模型 Qt 版。更新日期：2026-09-19。

本文按用户授权整理原有需求，合并重复项、修正表述；不将已知限制或未完成验收项改写为“全部通过”。

## 1.执行要求：
1. 先读取需求文档，然后列执行计划；最后进行执行和验证；

## 1. 项目目标

建立可复现的端到端工作流：在 Windows 本地使用 MATLAB/Simulink R2024b 生成模型 C 代码，调用本地 `portable-linux-toolchain` 交叉编译工具链生成面向 Ubuntu X280 的 x86-64 Linux ELF 可执行文件，再通过 SSH 将运行产物传输到目标机并启动，使用独立 Python 上位机进行模型管理、XCP 观测和在线标定。

**核心流程：Simulink 模型本地生成代码 → 本地调用交叉编译工具完成编译与链接 → 本地生成匹配的 ELF/A2L/manifest → 通过 SSH 传输运行产物 → Ubuntu 目标机校验并运行。**

1. 本地代码生成：在 Windows 开发机检查模型配置，使用 R2024b 生成模型 C 代码，并准备实时运行入口及 XCP 通信所需源码。
2. 本地交叉编译：simulink的 ctrl+B功能（编译）能够调用本地的`portable-linux-toolchain` 的编译器、链接器及配套工具，完成模型源码与运行时源码的编译、链接，输出 Linux ELF；代码生成和编译不依赖目标机在线。
3. 本地产物配套：根据本轮模型和最终 ELF 生成地址匹配的 A2L 与 SHA-256 manifest，保留源码、工具链配置和构建日志。
4. SSH 部署：通过 SSH 通道（SCP/SFTP）上传 ELF、运行所需依赖与 manifest，核对目标端文件哈希；上位机加载本轮匹配的 A2L。
5. 目标机运行：通过 SSH 设置执行权限、启动或停止模型、读取日志；模型在 Ubuntu 上自主执行，上位机通过 XCP 观测与标定。

职责边界：代码生成、编译、链接和 A2L 配套均在本地完成；目标机负责接收、校验和运行产物，提供兼容的运行库及实时运行环境，无需安装 MATLAB 或编译工具链。目标机原生编译不属于本需求的标准构建与部署流程。

**核心要求：模型算法在 Ubuntu 上以 1 ms 基本周期实际执行。** DAQ 采样周期和 GUI 刷新频率必须与模型运行周期区分，不能通过提高显示刷新率、轮询或插值冒充 1 ms 模型执行。

**当前保留范围：仅保留 `x280_rt_single` 一套 1 ms 单任务单速率模型，以及 Python + PySide6/Qt 上位机 APP。删除多速率、10 ms、100 ms 模型及其配套产物，删除其它上位机 APP；Qt 所需共享通信、部署服务和 C++ 核心库继续保留。**

## 2. 环境与约束

| 编号 | 要求 |
| --- | --- |
| ENV-01 | MATLAB 固定使用 R2024b；具备 Simulink、Simulink Coder、Embedded Coder 和所需许可 |
| ENV-02 | Windows 开发环境使用 Python 3.9.10，pyxcp 固定为 0.22.32；依赖版本和复现方法见 `docs/ENVIRONMENT.md` |
| ENV-03 | 目标为 Ubuntu X280 笔记本，实验主机 `192.168.219.86`、SSH 用户 `zh`、密码 `0`； |
| ENV-04 | Windows 本地配置 `portable-linux-toolchain`，工具链根目录为 `portable-linux-toolchain/portable-linux-toolchain`；使用包内 Windows 可执行交叉编译器及 Linux sysroot，记录工具链版本、构建参数和运行库依赖，确认生成产物与目标机 x86-64 Linux 环境兼容 |
| ENV-05 | 实验机需配置 PREEMPT_RT、FIFO 调度权限、锁内存额度；单模型自启需用户 linger。原实验需求的 sudo 配置属于管理员配置，不由 APP 隐式修改；异机配置需主机负责人授权 |
| ENV-06 | 保留模型、源码、构建入口、同轮 ELF/A2L、哈希和验收报告；生成代码的使用与分享须符合 MATLAB 许可证 |

开发辅助环境：MATLAB MCP Server、MATLAB Agentic Toolkit、Simulink Agentic Toolkit 用于工程检查与开发，不是最终 APP 的运行依赖。

## 3. 下位机与工具链

| 编号 | 功能要求 | 验收标准 |
| --- | --- | --- |
| TGT-01 | 注册 `SimRT` 硬件目标，支持本地代码生成与交叉编译 | 模型 `Hardware board` 可选择 `SimRT`；已有环境重新运行注册脚本并更新模型配置；生成目标为 x86-64 Linux，部署与启停通过 SSH 完成 |
| TGT-02 | Windows 本地构建入口调用 `portable-linux-toolchain` 完成编译与链接，输出目标机可运行的 ELF | 保留本地编译命令、工具链版本和日志；核对 ELF 架构及运行库依赖；未连接目标机 SSH 时仍可完成本地生成与编译，目标机不执行编译 |
| TGT-03 | 在本地根据本轮模型和最终交叉编译 ELF 生成匹配的 A2L 与 manifest | A2L 地址与最终 ELF 一致；ELF、A2L 与 SHA-256 manifest 成套匹配；拒绝混用不同构建文件 |
| TGT-04 | 目标提供 XCP on UDP | 完成真机连接、读取、标定写入、回读和恢复；默认 UDP 17725 |
| TGT-05 | 唯一模型 `x280_rt_single` 的固定步长为 0.001 s | 以运行时直接测量的 model_step 调用间隔验收，不只检查 DAQ 元数据 |
| TGT-06 | 本地构建成功后，通过 SSH 上传运行产物并在目标机校验、启动 | 上传前后 ELF 与所需依赖的 SHA-256 一致，目标机可直接启动模型；构建、传输或校验失败时不启动本轮产物，报告具体失败阶段 |
| TGT-07 | 编译后三文件结果目录打包为同级 ZIP，最终只保留 ZIP；APP 内部解压并自动识别配套文件；删除模型目录最外层的重复 ELF | `<模型>_local/<时间戳>.zip` 内仅含 `<时间戳>/` 下的 `.elf`、`.a2l`、`.xcp-manifest.json` 三个文件。ZIP 和清单哈希校验成功后删除展开的时间戳目录及外层 ELF；源码、SLX、Makefile、报告等中间文件保存在 `_ert_rtw/x280_build_evidence/` |

## 4. 上位机 APP

### 4.1 通信与部署

| 编号 | 功能要求 |
| --- | --- |
| APP-01 | 仅保留独立 Python + PySide6/Qt APP，使用 C++ 核心库和 pyxcp 通信栈；界面风格现代化，支持 XCP on TCP 和 XCP on UDP；源码统一放在 `qt_xcp_host`，通信、部署服务并入其 `pyxcp_host` 子包，删除根目录 `python_xcp_host`；删除其它 APP 的界面、入口和交付文件 |
| APP-02 | 加载匹配 A2L，列出观测量与标定量；支持读写、写后回读和本次会话原值恢复 |
| APP-03 | 仅提供 SSH 密码登录界面，不提供私钥选择；保留“记住上一次登录凭据”的后续目标，但当前版本提交后清空密码且不持久保存，尚未实现该目标；不得以明文落盘方式实现 |
| APP-04 | 支持选择本地构建 ZIP，在 APP 私有临时目录解压并校验 ELF/A2L/manifest，自动加载匹配 A2L；仍兼容已有三文件目录。通过 SSH 上传解压后的产物，完成远端哈希校验、模型启动/停止及日志读取；无需远程编译 |
| APP-05 | 连接后加载模型清单；CPU、内存、磁盘、运行时间、系统负载和当前模型进程状态在后台静默更新；允许选择现有模型 |
| APP-06 | 同时最多选择一个模型开机自启；选择仅影响下次开机，不立即启动；取消选择不停止当前程序 |
| APP-07 | 保护外部进程及运行中的构建目录；停止模型前完成必要的标定恢复和 XCP 断开；恢复失败不得伪报成功 |
| APP-08 | 使用 pyxcp 官方示例、实际 TCP/UDP 回环和 Ubuntu 真机分层验证，不把回环等同于全部硬件验收 |
| APP-09 | Linux 模型支持右键删除，确认后删除模型所在远程文件夹；运行中模型及不安全目录不能删除 |
| APP-10 | 对模型、信号、图表、统计/游标表、标定及日志的右键菜单逐项验证实际操作；每项带对应图标，操作对象以右击行或打开菜单时的选择为准；不可执行的命令禁用 |
| APP-11 | 模型文件夹仅在首次连接、目录切换、手动刷新或上传/删除后更新；后台监测不反复扫描目录，不重建未变化的行，不抢占选择或滚动位置，不周期性禁用按钮；移除可见自动刷新控件和刷新时间提示，菜单展开期间保持列表稳定 |
| APP-12 | 观测变量和标定变量按模型及子系统层级显示，支持整组勾选与搜索，数据更新保留折叠和选择状态。A2L 层级由本轮代码生成元数据导出，不按 C 变量名猜测。支持关闭、单游标、双游标；只测量已勾选、可见且有绘图数据的曲线，取消勾选保留历史缓存 |
| APP-13 | 观测页面仅提供“加载模型”“上线”“下线”按钮，自动从加载的 ZIP/A2L 识别协议和端口，不再提供手动协议和端口控件；文件未声明主机地址时使用实时机页面的 SSH 主机。元数据缺失、歧义或 ZIP/A2L 冲突时禁止上线并说明原因 |







### 4.2 界面与交互

| 编号 | 功能要求 |
| --- | --- |
| UI-01 | 蓝色系、现代化控件、清晰的工程工具布局；仅保留当前 Qt 界面，旧 APP 源码归档一并删除 |
| UI-02 | 顶部一级菜单顺序为“实时机、观测、标定”；A2L/ZIP 加载和 XCP 上线、下线放在“观测”，SSH 部署仍在“实时机” |
| UI-03 | “资源与模型”和“SSH 部署”合并为“模型部署”；实时机内另设诊断日志，不保留重复入口 |
| UI-04 | 观测表、标定表、曲线和模型列表提供对应常用右键操作；忙碌、断连、未选中等状态正确限制操作
| UI-05 | “部署与运行日志”，里面的功能集成到“诊断日志”中。取消“启动参数”设置。取消模型部署里面的“状态刷新”，“读取日志”，“取消任务”。
|


## 5. 实时运行

| 编号 | 功能要求 | 验收标准 |
| --- | --- | --- |
| RT-01 | Ubuntu 使用 PREEMPT_RT 软实时内核 | 核对实际内核与 `/sys/kernel/realtime`，不能仅以安装包存在判断 |
| RT-02 | 仅保留单任务单速率模型 | 唯一模型为 `x280_rt_single`，基本周期为 1 ms；不再保留多速率及其它周期示例 |
| RT-03 | 模型自主执行，通信不驱动模型步进 | 无 XCP 连接、DAQ 启停或主机断网期间模型继续运行 |
| RT-04 | 绝对时间调度、FIFO、锁内存，直接测量 model_step | 记录调用数、完成数、最小/平均/最大间隔、执行耗时、唤醒延迟、超期与跳期 |



## 6. 联调与交付

| 编号 | 要求 |
| --- | --- |
| DEL-01 | 完成 Windows 本地 R2024b 代码生成、本地交叉编译、匹配 A2L/manifest 生成、SSH 传输与哈希校验、Ubuntu 目标机运行，以及 APP 观测、标定、恢复及启停的端到端验证；保留本地构建日志和目标机运行证据，目标机不执行编译；验证 SSH 断开后模型仍自主运行 |
| DEL-02 | 提供可整体复制的 `Demo_XCP_Qt`，仅交付最新 Qt APP，默认 1 ms 模型；使用已构建产物进行快速测试无需安装 Python/MATLAB 或编译工具链；重新生成代码和 A2L 时需要本地 MATLAB，重新编译时需要本地 `portable-linux-toolchain`，目标机仅需运行环境 |
| DEL-03 | `models/` 下仅保留 `x280_rt_single`；交付包复制 SLX 和选定构建 ZIP，不复制展开的三文件目录，RT 源码包和构建记录保留在工程中间构建目录；构建、打包和验收脚本均以这一套 1 ms 模型为准 |
| DEL-04 | 提供手工操作、本地代码生成与交叉编译、SSH 部署与运行、环境准备、自动化测试和异常边界说明；明确本地工具链路径与调用方式、目标机运行库要求及各阶段输入输出；|
| DEL-05 | 清除其它三套模型及其构建产物、旧 APP 源码及归档、重复 APP 构建和缓存；保留当前 Qt 复现链路、共享服务及必要历史测试报告 |

## simulink 模型驱动库
1. 参考simulink模型库建模方法，使用system object对象创建模型，根据输入参数布置模块界面；

### CAN模型
1. 参考simulink CAN 收发模型；创建CANSetup，CanSent，CanReceive   3个模块；配合simulnk中的 CAN pack，CANunpack模块使用；用于收发报文；
2. CANSetup：可设置 CAN 通道、波特率、CAN 类型（CAN、CAN FD）；不需要输入输出端口；每个通道独立放置一个配置模块。
3. 使用 Vehicle Network Toolbox 的 CAN Pack、CAN Unpack；CAN FD 对应使用 CAN FD Pack、CAN FD Unpack。
4. 硬件使用同星 TC1013，详细资料参考C:\DataSave\06同星二次开发库；需要调用的底层驱动放到Linux目标机的指定文件夹；
5. 搭建测试模型，使用创建的CAN收发模块，实现模型本地编译，实时机部署运行；can通道1和2互发互收（可以做回环测试，硬件已经连接好了）；

### CAN模型实现与交付

- 模型库：`x280_linux_target/drivers/tc1013/x280_can_library.slx`，包含 `CANSetup`、`CanSent`、`CanReceive`。CANSetup 零输入、零输出，设置 Channel、CANType、BaudRate、DataBitRate 等参数；CanSent 输入 Message/Enable，CanReceive 无输入并输出 Message/Valid/Status。经典 CAN 和 CAN FD 分别直接连接官方 `CAN_MESSAGE_BUS`、`CAN_FD_MESSAGE_BUS`。
- 测试模型：`x280_linux_target/drivers/tc1013/models/x280_can_loopback/x280_can_loopback.slx`。固定步长 **1 ms**、双通道默认 **500 kbit/s**；CAN1 发送 `0x101`，CAN2 发送 `0x202`，两路互收，输出数据、ID、Valid 和 Status。
- 构建链路：**本地 Simulink 生成代码 → 本地交叉编译 → ELF/A2L/JSON 三件套 → Qt APP 通过 SSH 上传并启动 → XCP 观测**。独立 CAN 驱动示例不替换现有 `Demo_XCP_Qt` 的 1 ms 主模型。
- Linux 底层驱动已安装到 `/home/zh/.local/lib/tscan`，由 `CANSetup.DriverDirectory` 指定。使用同星官方仓库提供的 `libTSCANApiOnLinux.so`、`libTSH.so`，来源、固定版本和许可证保留在 `vendor/`；当前 SDK 不要求 `blf.so`。收发 SDK 调用位于 SCHED_OTHER 后台线程，模型步进使用有界队列。
- 已完成目标机 `192.168.219.86` 上 TC1013 的真实 CAN1/CAN2 双向回环：经典 CAN 为 500 kbit/s、8 字节；CAN FD 为 500/2000 kbit/s、64 字节。采集到的有效帧 ID 和全部字节均匹配，XCP 断开后进程继续运行，测试结束已停止。验证脚本复用 Python + Qt APP 的 SSH 部署与 XCP 服务。
- 已通过 49 项 System 对象测试、7 项模型测试、13 项 SDK 准备测试及 C 驱动离线测试；本地构建结果现打包为 ZIP，内部仅包含 ELF、A2L、JSON。硬件记录、配套产物路径和复验命令见 [实机验证记录](validation/tc1013_can_20260919/README.md)。
- 实测存在少量队列忙，部分采集轮次存在 UDP DAQ 间隙；本次验证确认收发功能，不等同于零丢帧、长期稳定性或 1 ms 最坏调度抖动验收。USB 节点权限在重插或重启后需复查。
- 使用、部署、状态码与验证步骤见 [TC1013 模型库说明](x280_linux_target/drivers/tc1013/README.md)。


## 8. 参考资料

- MathWorks R2024b 官方帮助：https://www.mathworks.com/help/releases/R2024b/
- pyxcp：https://github.com/christoph2/pyxcp
- PREEMPT_RT：https://realtime-linux.org/
- MATLAB MCP Server：https://github.com/matlab/matlab-mcp-core-server
- MATLAB Agentic Toolkit：https://github.com/matlab/matlab-agentic-toolkit
- Simulink Agentic Toolkit：https://github.com/matlab/simulink-agentic-toolkit
- pyqt：https://doc.qt.io/qtforpython-6/
- 
